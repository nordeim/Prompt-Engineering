I'll analyze the three LuxeVerse landing page workflow examples as a triangulated dataset, extracting the invariant methodology behind their design-and-build process. Let me begin with deep analysis.
Now I'll synthesize the distilled system prompt and save it to the output directory
---

## Distillation Complete

**[Download Distilled System Prompt](sandbox:///mnt/agents/output/luxeverse-landing-prompt.md)**

---

### What Was Extracted

The three LuxeVerse landing page workflows were treated as a **triangulated dataset** — not as individual examples to imitate, but as repeated manifestations of an underlying operating system. Here's what was distilled:

| Invariant Element | How It Was Generalized |
|---|---|
| **6-Phase Sequence** | Preserved as mandatory workflow: ANALYZE → PLAN → VALIDATE → IMPLEMENT → VERIFY → DELIVER |
| **Anti-Generic Mandate** | Converted from example-specific prohibitions (no purple, no bento) to a doctrine with generalizable rules |
| **OKLCH Color System** | Extracted as a universal principle: "perceptually uniform color space, never mix with hex/RGB/HSL" |
| **Golden Ratio Spacing** | Generalized from `--space-md: 1.618rem` to "base all spacing tokens on φ" |
| **Cinematic Motion** | Distilled from specific `cubic-bezier` values to principles: "serve narrative pacing, not decoration" |
| **Accessibility Foundation** | Extracted from repeated patterns into a comprehensive checklist with 13 gates |
| **Performance as Luxury** | Generalized from "<150KB" to "single file, zero external JS, passive listeners" |
| **Stakeholder Checkpoint** | The VALIDATE phase — present decisions in tables, request approval before code |

### What Was Removed (Incidental)

- **Specific font pairings** (Cormorant Garamond + DM Sans vs. Playfair Display + Crimson Pro) → generalized to "high-contrast serifs for display, refined sans-serifs for UI"
- **Section names** ("Midnight Atelier", "First Light") → removed; the prompt instructs selecting a named concept per-project
- **Specific image URLs** → removed; the prompt instructs using Unsplash with proper alt text
- **Tailwind CDN usage debate** → generalized to "single file with inline CSS unless Next.js explicitly requested"
- **Hero copy examples** → removed; the prompt instructs evocative, brand-appropriate copy

### What Was Added (Behavioral Fidelity)

- **Explicit NEVER/ALWAYS guardrails** — the examples implied these through critique, but the prompt makes them contractual
- **Verification checklist** — the examples performed QA ad-hoc; the prompt encodes it as a mandatory phase
- **Ambiguity handling rules** — "When PRD is unclear, prefer cinematic editorial over commercial efficiency"
- **Output contract** — explicit MUST/MUST NOT for deliverable format

### Structural Integrity

The prompt is organized as an **operational directive**:

1. **Role & Identity** — establishes authority and specialization
2. **Primary Mission** — defines the core objective in one paragraph
3. **Operating Principles** (6 doctrines) — the invariant rules that govern all decisions
4. **Workflow** (6 phases) — the mandatory sequence with inputs/outputs for each phase
5. **Verification / Self-Check** — 13 quality gates as a checklist
6. **Output Contract** — explicit MUST/MUST NOT for deliverables
7. **Guardrails** — NEVER/ALWAYS constraints with behavioral consequences
8. **Final Principle** — the philosophical anchor

The prompt is **~9,100 characters, 188 lines** — dense enough to preserve rigor, lean enough to avoid bloat. It will work across Claude, GPT-4, Gemini, and other capable LLMs without model-specific tuning.
